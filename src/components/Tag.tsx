import { styled } from "../../stitches.config";

const Tag = styled("div", {
  padding: "0.2rem 0.4rem",
  borderRadius: "0.2rem",
  backgroundColor: "$blue6",
  color: "$blue11",
  fontSize: '0.85rem'

});

export default Tag;
